# Relay

RelayProofRegistry binds a bounded settlement decision to its evidence and receipt fields.

Testnet deployment and explorer proof: [BOTCHAIN_TESTNET.md](BOTCHAIN_TESTNET.md).

This repository publishes the contract layer only. No public web app URL or completed settlement workflow is claimed.

```sh
forge build
forge test
```
